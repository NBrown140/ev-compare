{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "EV Compare Sources",
  "description": "Maps vehicle IDs to their data sources with archived URLs",
  "type": "object",
  "additionalProperties": {
    "type": "array",
    "minItems": 1,
    "items": {
      "type": "object",
      "required": ["url", "archived_url", "accessed"],
      "additionalProperties": false,
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Original source URL"
        },
        "archived_url": {
          "type": "string",
          "format": "uri",
          "pattern": "^https://web\\.archive\\.org/web/",
          "description": "Wayback Machine archived URL"
        },
        "accessed": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date the source was accessed (YYYY-MM-DD)"
        },
        "fields": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Optional list of CSV fields this source covers"
        }
      }
    }
  }
}
